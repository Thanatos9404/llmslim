# Production Studio runs the exact published release. Hosted inference remains
# disabled unless its server-only secrets and durable quotas are configured.
numpy>=1.21
scikit-learn>=1.0
llmslim[sarvam,mongodb]==0.6.0
