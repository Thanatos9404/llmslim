# v0.5.0 release validation

Date: 2026-09-13

- Local Python 3.12: 507 tests passed; 90.62% branch coverage.
- Official MCP 2.2.0: in-process, localhost Streamable HTTP, and literal-argv stdio tests passed.
- OpenAI Agents SDK 0.22.2: host callback and mismatched-plan regression tests passed.
- Ruff lint and formatter passed. MyPy checked 28 package files with silent dependency following.
- Wheel and sdist built; Twine passed; contents inspected; isolated wheel import and MCP static plan passed.
- Next.js production build and TypeScript passed. ESLint has no errors; existing unused-code warnings remain.
- Legacy regression script completed with compression-ratio warnings against its historical baseline. It is not evidence of a v0.5.0 compression improvement.
- New protections: refresh revokes stale cache entries, cache results are isolated copies, pagination bytes are bounded before following a cursor, and Agents bridge plans must match exactly.
- Studio restores request timeout/cancellation, discards results after input changes, and handles clipboard failures.
- Existing robots, sitemap generator, verification assets, canonical and metadata support preserved. Release version metadata updated.
- CI matrix, publication and live verification are checked separately during release execution.

Limits: SELECTIVE remains research-only. Full/measure-only plans save zero schema tokens. No live-model task-success or Sarvam latency/cost claims are made. Python 3.10+ is required for optional MCP/Agents extras.
