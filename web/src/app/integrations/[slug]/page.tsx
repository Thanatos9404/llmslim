import Link from "next/link"
import { notFound } from "next/navigation"
import { AlertTriangle, ArrowLeft, CheckCircle2, Cpu, Layers, ShieldCheck, Zap } from "lucide-react"
import { DocCodeBlock } from "@/components/docs/DocCodeBlock"
import { IntegrationIcon } from "@/components/integrations/IntegrationIcon"
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion"
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"
import { INTEGRATIONS_REGISTRY } from "@/data/integrations"
import { constructMetadata } from "@/lib/seo"

export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const item = INTEGRATIONS_REGISTRY[slug]; return item ? constructMetadata({ title: `LLMSlim + ${item.name} Integration Guide — Prompt & Context Compression`, description: item.description }) : constructMetadata({ title: "Integration Not Found | LLMSlim", description: "Requested platform integration not found." }) }
export async function generateStaticParams() { return Object.keys(INTEGRATIONS_REGISTRY).map((slug) => ({ slug })) }

export default async function IntegrationSlugPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params; const item = INTEGRATIONS_REGISTRY[slug]; if (!item) notFound()
  return <article className="mx-auto max-w-4xl space-y-8"><Button variant="ghost" size="sm" render={<Link href="/integrations" />}><ArrowLeft /> Back to integrations</Button><header className="space-y-4"><div className="flex items-center gap-4"><Card className="p-3"><IntegrationIcon iconKey={item.iconKey} className="size-8" /></Card><div><div className="flex flex-wrap gap-2"><Badge>{item.category}</Badge><Badge variant="outline">{item.badgeText}</Badge></div><h1 className="mt-2 text-4xl font-semibold tracking-tight">LLMSlim + {item.name}</h1></div></div><p className="text-lg text-muted-foreground">{item.tagline}</p><p className="text-muted-foreground">{item.description}</p></header><Guide title="1. Package installation" icon={Zap}><p className="text-muted-foreground">Install LLMSlim and the official {item.name} SDK using your package manager.</p><DocCodeBlock language="bash" filename="terminal" code={item.installation.command} /></Guide><Guide title="2. Architecture & execution flow" icon={Layers}><Card><CardContent className="space-y-3">{item.architectureFlow.map((step, index) => <div key={step} className="flex gap-3"><Badge variant="secondary">{index + 1}</Badge><p className="text-sm text-muted-foreground">{step}</p></div>)}</CardContent></Card></Guide><Guide title="3. Production code pattern" icon={Cpu}><DocCodeBlock language={item.codeExample.language} filename={item.codeExample.filename} code={item.codeExample.code} /></Guide><Guide title="4. Deployment best practices" icon={ShieldCheck}><Card><CardContent className="text-muted-foreground">{item.deploymentGuide}</CardContent></Card></Guide><Guide title="5. Key optimization tips" icon={CheckCircle2}><Card><CardContent><ul className="list-disc space-y-2 pl-5 text-muted-foreground">{item.optimizationTips.map((tip) => <li key={tip}>{tip}</li>)}</ul></CardContent></Card></Guide>{item.benchmarks.length > 0 && <Guide title="6. Performance metrics"><Card><CardContent className="overflow-x-auto"><Table><TableHeader><TableRow><TableHead>Metric</TableHead><TableHead>Uncompressed</TableHead><TableHead>LLMSlim</TableHead><TableHead>Impact</TableHead></TableRow></TableHeader><TableBody>{item.benchmarks.map((row) => <TableRow key={row.metric}><TableCell>{row.metric}</TableCell><TableCell>{row.uncompressed}</TableCell><TableCell>{row.compressed}</TableCell><TableCell>{row.impact}</TableCell></TableRow>)}</TableBody></Table></CardContent></Card></Guide>}{item.faqs.length > 0 && <Guide title="Frequently asked questions"><Card><CardContent><Accordion>{item.faqs.map((faq) => <AccordionItem key={faq.question} value={faq.question}><AccordionTrigger>{faq.question}</AccordionTrigger><AccordionContent>{faq.answer}</AccordionContent></AccordionItem>)}</Accordion></CardContent></Card></Guide>}{item.troubleshooting.length > 0 && <Guide title="Troubleshooting" icon={AlertTriangle}>{item.troubleshooting.map((entry) => <Alert key={entry.issue}><AlertTriangle /><AlertTitle>{entry.issue}</AlertTitle><AlertDescription>{entry.solution}</AlertDescription></Alert>)}</Guide>}</article>
}

function Guide({ title, icon: Icon, children }: { title: string; icon?: typeof Zap; children: React.ReactNode }) { return <section className="space-y-4 scroll-mt-24"><h2 className="flex items-center gap-2 text-2xl font-semibold tracking-tight">{Icon && <Icon className="size-5" />}{title}</h2>{children}</section> }
