# Production Studio runs the exact bundled 0.7.0 wheel. Hosted inference remains
# disabled unless its server-only secrets and durable quotas are configured.
numpy>=1.21
scikit-learn>=1.0
./vendor/llmslim-0.7.0-py3-none-any.whl[sarvam,mongodb]
