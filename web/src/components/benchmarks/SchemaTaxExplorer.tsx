"use client"
import { useMemo, useState } from "react"
import { Braces, ShieldCheck } from "lucide-react"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"
import type { SchemaComplexity, SchemaTaxCatalog } from "@/lib/phase2-data"
const counts = [1, 4, 8, 16, 32, 64]; const complexities: SchemaComplexity[] = ["SIMPLE", "MEDIUM", "COMPLEX"]
const tokens = (value: number) => new Intl.NumberFormat("en-US").format(value)
export function SchemaTaxExplorer({ catalogs }: { catalogs: SchemaTaxCatalog[]; compact?: boolean }) {
  const [toolCount, setToolCount] = useState(64); const [complexity, setComplexity] = useState<SchemaComplexity>("COMPLEX"); const catalog = useMemo(() => catalogs.find((item) => item.tool_count === toolCount && item.complexity === complexity) ?? catalogs[0], [catalogs, complexity, toolCount]); if (!catalog) return null
  return <Card><CardHeader><Badge variant="secondary" className="w-fit">Research track · not shipped</Badge><CardTitle className="mt-3">The schema tax is visible.</CardTitle><CardDescription>Measured synthetic tool contracts show the cost of resending a whole catalog. This is measurement, not schema compression, selection, or lazy loading.</CardDescription></CardHeader><CardContent className="space-y-6"><div className="grid gap-4 sm:grid-cols-2"><div className="space-y-2"><label className="text-sm font-medium">Catalog tools</label><Select value={String(toolCount)} onValueChange={(value) => setToolCount(Number(value))}><SelectTrigger className="w-full"><SelectValue /></SelectTrigger><SelectContent>{counts.map((count) => <SelectItem key={count} value={String(count)}>{count} tools</SelectItem>)}</SelectContent></Select></div><div className="space-y-2"><label className="text-sm font-medium">Schema complexity</label><Select value={complexity} onValueChange={(value) => setComplexity(value as SchemaComplexity)}><SelectTrigger className="w-full"><SelectValue /></SelectTrigger><SelectContent>{complexities.map((item) => <SelectItem key={item} value={item}>{item[0] + item.slice(1).toLowerCase()}</SelectItem>)}</SelectContent></Select></div></div><div className="grid gap-3 sm:grid-cols-3"><Metric label="Full catalog" value={`${tokens(catalog.total_schema_tokens)} tokens`} /><Metric label="Tokens per tool" value={tokens(catalog.mean_tokens_per_tool)} /><Metric label="16 turns · derived" value={`${tokens(catalog.cumulative_schema_tokens["16"]?.tokens ?? 0)} tokens`} /><Metric label="32 turns · derived" value={`${tokens(catalog.cumulative_schema_tokens["32"]?.tokens ?? 0)} tokens`} /></div><div className="flex gap-2 text-sm text-muted-foreground"><ShieldCheck className="size-5 shrink-0" />Contracts retain names, parameter types, required fields, enums, nested shape, constraints, and meaningful defaults in the research comparison.</div></CardContent></Card>
}
function Metric({ label, value }: { label: string; value: string }) { return <Card size="sm"><CardHeader><CardDescription>{label}</CardDescription><CardTitle>{value}</CardTitle></CardHeader></Card> }
