import Link from "next/link"
import { notFound } from "next/navigation"
import { ArrowLeft, CheckCircle2, FileCode, Server, TriangleAlert } from "lucide-react"
import { DocCodeBlock } from "@/components/docs/DocCodeBlock"
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"
import { BENCHMARK_SUITES } from "@/data/benchmarks"
import { constructMetadata } from "@/lib/seo"

export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const suite = BENCHMARK_SUITES[slug]; return suite ? constructMetadata({ title: `${suite.title} — Open & Reproducible Benchmark`, description: `${suite.subtitle}. ${suite.description}` }) : constructMetadata({ title: "Benchmark Not Found | LLMSlim", description: "Requested benchmark suite not found." }) }
export async function generateStaticParams() { return Object.keys(BENCHMARK_SUITES).map((slug) => ({ slug })) }
export default async function BenchmarkSlugPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const suite = BENCHMARK_SUITES[slug]; if (!suite) notFound(); const environment = Object.entries(suite.environmentSpec); return <article className="mx-auto max-w-5xl space-y-8"><Button variant="ghost" size="sm" render={<Link href="/benchmarks" />}><ArrowLeft /> Back to benchmarks</Button><header><div className="flex flex-wrap gap-2"><Badge>Target: {suite.targetModel}</Badge><Badge variant="outline">Baseline: {suite.baselineName}</Badge></div><h1 className="mt-4 text-4xl font-semibold tracking-tight">{suite.title}</h1><p className="mt-3 text-lg text-muted-foreground">{suite.subtitle}</p><p className="mt-2 text-muted-foreground">{suite.description}</p></header><Card><CardHeader><CardTitle className="flex items-center gap-2"><Server /> System environment</CardTitle></CardHeader><CardContent className="grid gap-3 sm:grid-cols-2">{environment.map(([key, value]) => <div key={key} className="rounded-md border p-3"><p className="text-xs uppercase text-muted-foreground">{key.replace(/([A-Z])/g, " $1")}</p><p className="mt-1 text-sm font-medium">{String(value)}</p></div>)}</CardContent></Card><section><h2 className="mb-4 text-2xl font-semibold">Empirical benchmark matrix</h2><Card><CardContent className="overflow-x-auto"><Table><TableHeader><TableRow><TableHead>Method</TableHead><TableHead>Token reduction</TableHead><TableHead>Latency</TableHead><TableHead>Cost</TableHead><TableHead>Semantic retention</TableHead><TableHead>Instruction retention</TableHead><TableHead>Entity preservation</TableHead></TableRow></TableHeader><TableBody>{suite.tableData.map((row) => <TableRow key={row.method}><TableCell>{row.method}</TableCell><TableCell>{row.tokenReduction}</TableCell><TableCell>{row.executionLatency}</TableCell><TableCell>{row.billedCost10kReq}</TableCell><TableCell>{row.semanticRetention}</TableCell><TableCell>{row.instructionRetention}</TableCell><TableCell>{row.entityPreservation}</TableCell></TableRow>)}</TableBody></Table></CardContent></Card></section><Card><CardHeader><CardTitle className="flex items-center gap-2"><CheckCircle2 /> Key insights</CardTitle></CardHeader><CardContent><ul className="list-disc space-y-2 pl-5 text-muted-foreground">{suite.keyInsights.map((insight) => <li key={insight}>{insight}</li>)}</ul></CardContent></Card><Alert><TriangleAlert /><AlertTitle>Limitations and non-recommended workloads</AlertTitle><AlertDescription><ul className="mt-2 list-disc space-y-1 pl-5">{suite.limitations.map((limit) => <li key={limit}>{limit}</li>)}</ul></AlertDescription></Alert><Card><CardHeader><CardTitle>Experimental methodology</CardTitle><CardDescription>{suite.methodology}</CardDescription></CardHeader></Card><section><h2 className="flex items-center gap-2 text-2xl font-semibold"><FileCode /> Raw evaluation dataset</h2><DocCodeBlock language="json" filename="raw_dataset_sample.json" code={suite.rawDatasetJson} /></section><section><h2 className="flex items-center gap-2 text-2xl font-semibold"><FileCode /> Reproducible Python script</h2><DocCodeBlock language="python" filename="benchmark_reproducible.py" code={suite.reproducibleScript} /></section></article> }
